<div id="container" class="cls-container">
    <div class="cls-header cls-header-lg">
        <div class="cls-brand">
	        <a class="box-inline" href="/">
		        <span class="brand-title">Aeupdates<span class="text-thin">  service for aescripts.com</span></span>
    			</a>
          <div class="main_menu__block text-thin" style="display: inline">
              {{CurrentUser}}
                <span class="main_menu__menu-element">{{#link-to "users"}}Users{{/link-to}}</span>
                <span class="main_menu__menu-element">{{#link-to "manage"}}Manage{{/link-to}}</span>
                {{#if session.isAuthenticated}}
                    <span class="main_menu__menu-element" {{action 'logout'}}> Logout </span>
                {{else}}
                    <span class="main_menu__menu-element">{{#link-to "login"}}  Login  {{/link-to}}</span>
                {{/if}}
            </div>
        </div>
    </div>
    {{outlet}}
</div>
